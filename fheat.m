function VolumeForce = fheat(x,t)
 %VolumeForce = ones(size(x,1),1);
VolumeForce = zeros(size(x,1),1);
% if (x(1)-2)^2+(x(2)-2)^2 <=0.5
% VolumeForce = ones(size(x,1),1)*t^(-1)*10^-5;
% else
%     VolumeForce = zeros(size(x,1),1);
% end
end

